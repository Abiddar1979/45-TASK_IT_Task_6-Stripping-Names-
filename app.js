"use strict";
/* Task # 06  :Stripping Names: Store a person's Name, and include some whitespace characters at the beginning and end of the name.Make sure you use each character combination , "\t" and "\n", at least once .Print the name once, so the whitespace around the name is displayed. Then print the name after stripping the Whitespace*/
let Namewithwhitespace = "\n\t AFFAN \t\n";
console.log(Namewithwhitespace);
let Namewithoutwhitespace = Namewithwhitespace.trim();
console.log(Namewithwhitespace);
